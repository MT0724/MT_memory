#给定一个数组 nums 和一个值 val，你需要原地移除所有数值等于 val 的元素，返回移除后数组的新长度。
def removeVal(list,val):
	length=len(list)
	while val in list:
		list.remove(val)
	return len(list)
l=[2,4,3,4,4,9,1]
v=4
t=removeVal(l,v)
print('去除特定值4后该列表中还有元素个数：')
print(t)
